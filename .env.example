# 复制此文件为 .env.local 并填入真实 key（.env.local 已在 .gitignore 中，不会被提交）
#
# 多供应商支持：你只要配置任意一个或多个 *_API_KEY，前端模型选择下拉就会自动出现对应选项。
# 完全不影响：没配的供应商前端看不到，也不会报错。

# ============ DeepSeek（推荐，性价比高）============
# 申请：https://platform.deepseek.com
# 注意：兼容老的 OPENAI_API_KEY 字段名（如果你已经用这个名字配过 DeepSeek key，不必改）
DEEPSEEK_API_KEY=sk-your-deepseek-key

# ============ OpenAI 官方 ============
# 申请：https://platform.openai.com
# 注意字段名是 OPENAI_OFFICIAL_API_KEY（而不是 OPENAI_API_KEY，后者保留给 DeepSeek 兼容）
# OPENAI_OFFICIAL_API_KEY=sk-your-openai-key

# ============ Moonshot 月之暗面 ============
# 申请：https://platform.moonshot.cn
# MOONSHOT_API_KEY=sk-your-moonshot-key

# ============ Zhipu 智谱（GLM-4-Flash 免费）============
# 申请：https://open.bigmodel.cn
# ZHIPU_API_KEY=your-zhipu-key

# ============ Qwen 通义千问 ============
# 申请：https://dashscope.console.aliyun.com
# QWEN_API_KEY=sk-your-qwen-key

# ============ Google Gemini（免费，需科学上网）============
# 申请：https://aistudio.google.com/apikey
# 注意：本地 dev 在中国大陆需要代理；Vercel 部署在海外可直接访问
# GEMINI_API_KEY=your-gemini-key

# ============ 老配置（向后兼容，可不填）============
# 如果你只想用一个 OpenAI 兼容的 API（自定义 baseURL），保留下面 3 行即可：
# OPENAI_API_KEY=sk-xxx
# OPENAI_BASE_URL=https://api.deepseek.com
# OPENAI_MODEL=deepseek-chat
